rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read: if true;  // 모든 사용자가 읽기 가능
      allow write: if userId == 'sIOyGOi27KPY9794P20Z8dsq4Ap2';  // 특정 사용자만 쓰기 가능
    }
  }
} 